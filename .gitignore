

# ── Build artifacts ──────────────────────────────────────
bin/
.zig-cache/
zig-cache/
zig-out/
zig-out-win/
dist/
node_modules/
zig-pkg/
*.love
bundle.js
bundle-*.js
bundle-*.json
*.metafile.json
# Any .cache/ dir at any depth (editor backdrop ref-image overflow lives in
# cart/editor/.cache/ — machine-local, the referencing twig state never travels)
.cache/
pixel*.json
cart/editor/data/models/props/Model_10_2/paints/paint_1.json
# ── Vendored Zig toolchain ───────────────────────────────
# Run scripts/fetch-zig.sh after clone to populate. End-user rjit
# binaries from GitHub Releases already embed zig — only repo
# contributors need this on disk.
tools/zig/zig
tools/zig/lib/
tools/zig/cache/

# ── Staged postgres bundle ────────────────────────────────
# Run scripts/stage-pg-bundle to populate from the system postgres.
# Binaries from `scripts/ship` already embed this — only repo
# contributors need it on disk for dev mode.
.pg-bundle/


game models
cart/editor/data/
# The user's personal image-generation console (embedded API key) — never git's.
# The agent-skin skill drives it headlessly; it is not repo source.
cart/editor/img.cjs

# Compiled benchmark binaries (rebuilt from .zig source)
tsz/bench_lua_stress
tsz/bench_lua_worker

# Runtime state (sqlite dbs, supervisor queues)
.sweatshop/

# Composer user-captured/imported audio samples
cart/composer/samples/

# Third-party library downloads
blend2d-*.tar.gz
blend2d/

# ── Love2D stack (love2d/) ──────────────────────────────
love2d/cli/runtime/
love2d/.rjit-lint-cache.json

# Love2D example runtime copies (regenerated by `reactjit update`)
love2d/examples/*/reactjit/
love2d/examples/*/lua/
love2d/examples/*/lib/
love2d/examples/*/fonts/
love2d/examples/*/bin/
love2d/examples/*/data/

# Web landing page runtime copies
love2d/web/reactjit/
love2d/web/lua
love2d/web/lib/
love2d/web/fonts/
love2d/web/bin/
love2d/web/love/
love2d/web/node_modules/
love2d/web/logo.png

# Storybook runtime copies (reads from source-of-truth directly)
love2d/storybook/lua/
love2d/storybook/reactjit/
love2d/storybook/lib/
love2d/storybook/bin/
love2d/storybook/data/
love2d/storybook/love/data/

# Font binaries (managed by CLI)
love2d/fonts/
love2d/cli/runtime/fonts/

# QuickJS build artifacts
love2d/libquickjs/
love2d/quickjs/

# llama.cpp build artifacts
love2d/llama.cpp/
*.so
*.so.*
*.dylib
!love2d/lua/emulator/libagnes.so

# Platform binaries
love2d/bin/

# ── Zigos stack (tsz/) ─────────────────────────────────
# Generated Zig output from .tsz compiler
tsz/_gen_*.zig
tsz/generated_*
tsz/Inspector.gen.zig
tsz/generated_app.zig.bak
tsz/.tsz-preflight/
tsz/*.o
tsz/recording.mp4
tsz/screenshot.png
tsz/screenshots/
tsz/carts/autobahn-ws/reports/
tsz/carts/http-conformance/reports/
tsz/carts/inspector/Inspector.gen.zig

# ── TSZ stack (tsz/) ────────────────────────────────────
# Generated app output (compiler writes this)
tsz/runtime/generated_app.zig
tsz/runtime/ffi_libs.txt

# ── OS/Web binaries (large, rebuilt from source) ────────
os/web/initrd.cpio.gz
os/web/tsz-runtime.wasm
os/web/vmlinuz
os/web/v86/

# tsz/web/ OS images (TinyEMU/v86 binaries, rebuilt from source)
tsz/web/alpine-virt.iso
tsz/web/initrd.cpio.gz
tsz/web/vmlinuz
tsz/web/kernel-x86.bin
tsz/web/tsz-kernel.bin
tsz/web/buildroot-bzimage.bin
tsz/web/v86.wasm
tsz/web/libv86.js
tsz/web/seabios.bin
tsz/web/vgabios.bin

# Rust build artifacts (deps)
deps/vello_ffi/target/

# Web-demo OS images (rebuilt by emscripten/tinyemu build)
tsz/carts/web-demo/web-demo.app-web/alpine-virt.iso
tsz/carts/web-demo/web-demo.app-web/initrd.cpio.gz
tsz/carts/web-demo/web-demo.app-web/vmlinuz
tsz/carts/web-demo/web-demo.app-web/kernel-x86.bin
tsz/carts/web-demo/web-demo.app-web/tsz-kernel.bin

# ML models
experiments/llm/models/*.gguf

# Compiled binaries (root-level test artifacts)
test_and
test_and2

# Compiled runtime binaries (rebuilt from source)
archive/tsz-v1/runtime/bin/
tsz/runtime/bin/

# Root-level build artifact
generated_app.zig

# Claude ephemeral
.claude/scheduled_tasks.lock

# Cursor
.cursorignore

# Stray build/benchmark artifacts
app.o
Empty File
AGENT_ARCHITECTURE.md
benchmark_*.csv
# Leaked test-bundle temp outputs and root-level shot captures — root pngs
# are never source; keep the 2am checkpoint sweep from re-committing them
/_tmp_*
/*.png

# ── General ─────────────────────────────────────────────
# Generated files
src/generated/content.json
screenshot_*.png

# OS
.DS_Store
Thumbs.db

# Editor
.vscode/
*.swp
*.swo

# Cloned third-party sources
love2d/third_party/luajit/

# Environment variables & secrets
.env
.env.*
!.env.example

# Experiments
love2d/experiments/cuttlefish/*
!love2d/experiments/cuttlefish/setup.sh
os/cartridge-os/dev-key.*
os/cartridge-os/init_pubkey.h
os/cartridge-os/*.cart

# Local development
.claude/settings.local.json
.claude/local/
.claude-overflow/
*.local

# Logs and databases
*.log
npm-debug.log*
*.db
*.db-shm
*.db-wal
*.sqlite
*.sqlite-shm
*.sqlite-wal

# Testing
coverage/
.nyc_output/

# TypeScript
*.tsbuildinfo

# Vendor
love2d/vendor/

# Netlify
.netlify

# Game server binaries (too large)
love2d/examples/game-servers/source-server/
love2d/storybook/recording_*
love2d/storybook/screenshot_*

# Large binary assets
love2d/experiments/llm/models/*.gguf
love2d/storybook/love/fonts/cjk/
love2d/storybook/fonts/cjk/
love2d/data/*.db

# Experimental networking and test binaries
*_bin
# (was a bare `*bench`, which silently swallowed cart/input_bench,
# cart/polycss_effect_bench, scripts/bench AND any new dir ending in
# "bench" — e.g. editors/workbench. Scoped to the frozen tree it was for.)
tsz/experiments/**/*bench
tsz/experiments/bleeding/networking/test_api*
tsz/experiments/bleeding/networking/test_io*
tsz/experiments/bleeding/networking/quickjs/qjs_*_server
tsz/test_cart_linked

# Generated test artifacts
generated_*/
refactor_generated_*
tsz/handlers.zig
tsz/logic.zig
tsz/maps.zig
tsz/nodes.zig
tsz/state.zig
tsz/test_*.zig
tsz/test_min


# Test Screenshots
tsz/tests/*
deps/tinyemu
love2d/deps/love
deps/tinyemu
love2d/deps/love
tsz/tests/d121_check.png
tsz/tests/flex_proof.html
tsz/docs/CURSOR_IDE_TSZ_STRICT_AUTHORING_PLAN.md
tsz/docs/CURSOR_IDE_PRODUCT_SURFACE_OVERVIEW.md

# ── External deps / submodules (not ours) ───────────────
deps/tinyemu
deps/llama.cpp.zig
love2d/deps/love

# Vendored Zig toolchain (~338MB; downloaded, not source)
tools/zig/

# V8 prebuilt library (116MB, fetched by scripts/fetch-v8-prebuilt.sh)
deps/v8-prebuilt/*.a

# V8 runtime caches (filled when building the V8 path)
deps/zig-v8/.zig-cache/
deps/zig-v8/.lp-cache/
deps/zig-v8/v8/
deps/zig-v8/v8-build/

# Test screenshots (local artifacts)
tests/
/bundle*
/bundle-*/
$HOME


cache/
cache/*.js

# Generated thumbnails (path-encoded names)
*.thumb.jpg
/thumbnails/

# Root-level scratch/generated math notes
/layout_math*.md

# Oversized archive zips (stored on disk, too big for git)
/archive/editor.zip
/archive/experiments.zip
/archive/images.zip
/archive/os.zip

# Leftover untracked content from archived dirs (data, kept on disk)
/editor/
/experiments/
/os/
# Firecracker artifacts (kernel + built rootfs images)
images/

# Workspace-cart session scratch — per-cart autosaved working state (cutout,
# hmsc-int, ...). Runtime "disk = truth" state, written every few hundred ms
# while editing; never commit it, and keep file-trail watchers from churning on it.
cart/*/sessions/

# World Bible conditional-write artifacts. Confirmed pages are the direct
# `*.md` children; prepared temps, stable transaction locks, and versioned
# prior inodes stay local for crash/racing-editor recovery and must never enter
# the shipped corpus.
world/knowledge/*.md.tmp.*
world/knowledge/*.md.write-lock
world/knowledge/*.md.write-pending

# hmsc-int persistence content — runtime authoring state, never git's (the
# layer code in cart/hmsc-int/data/*.ts and the umbrella manifest are tracked).
# The live backing is cart/hmsc-int/data/domains/<domain>/store.db (sqlite,
# already caught by the *.db/*.db-wal/*.db-shm patterns above). Legacy root
# streams/snapshots are ignored while old worktrees drain.
cart/hmsc-int/data/streams/
cart/hmsc-int/data/snapshots/
cart/hmsc-int/data/domains/*/streams/
cart/hmsc-int/data/domains/*/snapshots/
# Compact-store safety backups (rjit game compact-store, req_1789) — the stale
# pre-compaction copies (e.g. _precompact_backup/model.store.db.bak, ~700MB);
# kept on disk as a recovery net, never git's. Matches at any depth under data/.
cart/hmsc-int/data/**/_precompact_backup/
cart/hmsc-int/data/**/_compact_backup/
# Editor-baked decal pixel files (DECALPIXFILE-0610) — derived cache the
# DecalPixelBaker regenerates from the stored DecalDocs; never git's.
cart/hmsc-int/data/decal-pixels/

# ── 3D models + image textures stay LOCAL — never published (USER RULING req_3772,
# "do not publish the 3d models or image based textures to github"; matches the
# GitHub-web directory deletions of 2026-08-03) ─────────────────────────────────
/cart/editor/data/models/
/cart/editor/data/textures/
/cart/hmsc-int/assets/tex/
/cart/hmsc-int/exports/*.png
/cart/hmsc-int/exports/.cache/
/cart/hmsc-int/texture.png
/cart/hmsc-int/Desk.glb
/cart/hmsc-int/car.obj
/pics/
/world/knowledge/
/tmp/
/design_handoff_color_studio/
/_MODEL_RECOVERY_BACKUP_20260629/
/research_runs/2026-07-28__auto-skinning-weights/

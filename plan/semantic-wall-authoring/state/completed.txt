Phase 1 — inventory verified: active source, runtime/native seams, tests, and reference-only prior-era evidence are mapped in INVENTORY.md.
Phase 2 — thesis verified: persisted semantic edges/openings, derived DCEL/outputs, shipped bake boundary, first-slice exclusions, and the done standard are explicit in THESIS.md.
Phase 3 — flow map verified: draw, opening mutation, topology, materials/anchors, prefabs, migration, live preview, frozen compile, play, and error paths are traced in FLOW_MAP.md.
Phase 4 — decomposition verified: every high-fragility active unit has a bounded destination, dependency direction, retained responsibility, and deletion boundary in DECOMPOSITION_MAP.md.
Phase 5 — reuse verified: canonical transaction, persistence, live-output, compile-cache, frozen-runtime, material, prefab, and semantic behaviors are separated from the fixed-module assumptions to retire in REUSE_MAP.md.
Phase 6 — execution plan verified: 158 ordered microsteps across contracts, native topology/mutation/compile, host wire, persistence/migration, commands, UI, frozen outputs, severance, and final proof pass validate_steps.sh with zero task-shaped rows.
Phase 6 amendment — building envelope sequence verified: the wall plan now establishes a general architecture facade/source/wire/bundle, while BUILDING_ENVELOPE_SEQUENCE.md specifies dual-sided floor slabs and footprint/plane-graph top roofs as later families.
Phase 6 amendment — vertical building grammar verified: floors are dual-sided slabs/ceilings, stairs/ramps/elevators are first-class vertical-link assemblies that own crossed-slab cutouts, spiral exits are placement parameters, elevators mutate one shaft/stops, and weather roofs cap only topmost envelopes.
Step 1 — wrote S001-011 preflight with timestamp, HEAD, complete git status snapshot, and strict section scope declaration; reopened the report and confirmed its verification fields.
Step 2 — wrote the canonical v1 ArchitectureSource wall schema, integer 16-u lattice law, stable directed-side and command-derived identity laws, explicit absolute support, derived-data exclusion, and architecture-family extension rule in contracts/source_schema.md; reopened the complete contract.
Step 3 — expanded source_schema.md with the exact measured architecture-kit declaration, measurement and outward-rounding law, stable/content identity split, hierarchy, structured query, atomic install flow, style-owned wall defaults, kit-owned opening occupancy, and prohibition on invented door/window dimensions; reopened and searched the contract fields.
Step 4 — wrote contracts/mutation_receipt.md with command/revision envelopes, typed stale rejection, complete record deltas, deterministic edge/opening/anchor remaps, face boundary lineage, reversible patches, half-open affected bounds, semantic dirty targets, and command-derived IDs; reopened the complete contract.
Step 5 — wrote contracts/compile_bundle.md with the versioned family section directory, canonical wall ordering, render/collider/material bands, door/portal and nav/audio/visibility facts, room faces, pick proxies, diagnostics, source/compiler/tuning/catalog hashes, and per-target hashes; reopened the complete contract.
Step 6 — wrote contracts/wire.md with the bounded little-endian header, packet/family/target tags, section directory and limits, UTF-8 string offsets, stable rejection envelope, unknown-section skip law, strict short/trailing/version rejection order, and ownership rules; corrected and reopened the RJAW little-endian magic.
Step 7 — wrote contracts/v4_migration.md with all six legacy aliases, exact captured meter rectangles and outward integer-cell mappings, oriented front/back to side-A/B law, maximal-run keys, stable legacy-derived IDs, untouched non-wall preservation, off-lattice rejection, and explicit ambiguity failures; reopened and searched the complete contract.
Step 8 — created framework/game/architecture_scale.zig as the checked 16-u authority with exact meter output and strict legacy-meter validation, imported it into stage_scale.zig through a named module dependency, and added compile-time parity assertions; both the scale unit test and the explicitly wired stage module test pass in ReleaseFast.
Step 9 — created wall_types.zig with contract-only lattice/source/receipt enums and structs, named limits/tuning with no opening dimensions, catalog references, and recursive allocator ownership; created building_catalog.zig with measured bounds, exact outward footprints, typed family/role/kind compatibility, wall-style and opening compatibility projections, strict paths/tags/hashes/masks, canonical queries, and owned deinit paths. Focused ReleaseFast catalog and type tests pass.
Step 10 — created the narrow building_architecture.zig facade, added allocator-owned integer-u source and scale smoke tests in framework/testing/unit/building_architecture.zig, registered test-building-architecture in build.zig, and wired the shared architecture-scale module into app/Scene3D/stage test roots without manufacturing std.Io; reopened every changed unit and git diff --check passes.
Step 11 — ran the Section A harness with repository Zig 0.16: test-building-architecture and test-stage-scale both exit 0; recorded the initial PATH-Zig mismatch and its resolution in the preflight/blocker reports, reopened the report, and prepared only explicit Section A paths for the logical commit.
Step 12 — expanded building_architecture.zig with the complete named facade surface for catalog validation/query, source validation, command application, compile, raycast, opening-slot enumeration, and v4 migration plus typed request scaffolds; unimplemented lanes reject explicitly, the facade imports no V8/renderer/editor/previous-era module, and the ReleaseFast harness passes.
Step 13 — added the red native source/catalog validation matrix for duplicate and missing IDs, invalid edge structure and scalar DTOs, unknown measured catalog references, measurement/footprint failures, typed family/role/kind authority, category/tag failures, and exact occupied/clearance-mask bounds and overlap; reopened the complete test file and kept allocator ownership explicit at every indexed validation boundary.
Step 14 — implemented exact DTO scalar rejection, bounded and globally unique stable IDs, allocator-backed vertex/edge indexes, source limits, floor/unit/dimension/reference/edge validation, measured-catalog lookup, and typed wall-style/opening-kind resolution in wall_types.zig and building_catalog.zig; the complete step-13 matrix passes under repository Zig 0.16 ReleaseFast with allocator cleanup and git diff --check is clean for the changed validation files.
Step 15 — created wall_topology.zig with whole-u points, widened vectors/cross products, orientation/collinearity/proper and endpoint intersection predicates, canonical rational points and parameters, exact rational ordering, collinear-overlap classification, and typed rejection of fractional or out-of-range lattice crossings; the standalone Zig 0.16 ReleaseFast compile passes and the reopened file is diff-clean.
Step 16 — added focused predicate tests for horizontal/vertical and diagonal crossings, parallel lines, collinear overlap, endpoint touch/on-segment behavior, full supported coordinate magnitude, negative coordinates, fractional off-lattice rejection, and canonical rational ordering; corrected the focused build graph to single-owner named modules, reopened the tests and wiring, and the repository Zig 0.16 ReleaseFast harness exits 0.
Step 17 — ran the pinned Zig 0.16 ReleaseFast architecture harness with full summary: 3/3 build steps and 15/15 tests passed; recorded every exact predicate case, command, timestamp, exit, runtime, and the absence of epsilon/atan2/rounding in S012-027-topology.md, then reopened the report and confirmed it is diff-clean.
Step 18 — added allocator-owned derived topology records for normalized vertices, directed half-edge twins, bounded faces, exterior boundary collections, contained holes, face references, index cycles, and typed diagnostics plus recursive deinit ownership at every level; reopened the declarations/deinit tail, confirmed diff cleanliness, and retained the 15/15 ReleaseFast pass.
Step 19 — added the red allocator-owned DCEL matrix for one square, two adjacent rooms, valid T and X junctions, disconnected squares sharing one floor exterior, a nested hole loop, a dangling exterior edge, and reversed source-edge direction with stable side-A orientation; reopened all fixtures/assertions and confirmed the test source is diff-clean before implementation.
Step 20 — implemented allocator-owned wall graph construction with indexed per-floor vertices, exact source-side twins, integer angular half-plane/cross-product sorting without atan2, left-face next/previous linking, bounded traversal, signed doubled area, strict containment, bounded-face/exterior/hole classification, and face assignment; corrected the exact-bound closure guard, added failure-safe partial-allocation cleanup, reopened all 754 lines, and the Zig 0.16 ReleaseFast harness passes 22/22 leak-checked tests.
Step 21 — added the red determinism fixture with four independently permuted vertex and edge arrays for the same two-room graph, comparing ordered face-signature bytes and the complete ordered diagnostic identity; reopened all four fixtures and comparison logic and confirmed the test file is diff-clean before canonicalization.
Step 22 — implemented canonical topology output ordering by floor, stable vertex/edge ID, source direction, and side; cycle discovery now supplies canonical rotation, face/hole signatures encode floor plus length-prefixed edge IDs and side direction without delimiter ambiguity, and exteriors follow canonical floor/cycle order. Reopened the ordering/signature code, confirmed diff cleanliness, and all 23/23 ReleaseFast tests pass across four source permutations.
Step 23 — added the red degenerate-input matrix for reversed duplicate coincident edges, partial collinear overlap, an explicitly excluded self-loop, a proper crossing inside a declared opening clearance interval, and a deliberately bounded face walk that cannot close; every assertion requires an ordered typed diagnostic with stable subject IDs and confirms retained versus explicitly excluded half-edge counts, and the reopened test file is diff-clean.
Step 24 — implemented canonical typed diagnostics with stable sorted subject IDs for duplicate coincident spans, partial collinear overlap, self-loops, unsplit proper crossings, measured opening-clearance crossings, and bounded traversal failure; a floor/AABB sweep prunes pair inspection, exact widened projection tests clearance columns, self-loops are explicitly named before exclusion, all other bad edges remain represented, diagnostics own/deinit all strings, and the reopened 1,207-line module passes 28/28 ReleaseFast leak-checked tests with diff-clean output.
Step 25 — wired the private wall_topology module into building_architecture.validateSource after structural and measured-catalog validation; the facade now constructs and always destroys normalized topology, returns typed topology_invalid on any explicit diagnostic, and exposes no graph internals. Added valid and duplicate-span facade traps, reopened facade/build/test wiring, confirmed diff cleanliness, and the ReleaseFast allocator harness passes 29/29.
Step 26 — promoted canonical face identity to SHA-256 over length-prefixed boundary records, pinned the two-room goldens in the four-permutation test, ran the pinned Zig 0.16 ReleaseFast harness twice at 29/29 with exits 0, and recorded both timestamps/summaries plus ordered hashes c176f07e…ea5d and 52f3e582…6d34 in S012-027-topology.md; reopened the report/hash code and confirmed diff cleanliness.
Step 27 — reopened wall_topology.zig after the deterministic gate and confirmed it contains no V8, world-loader, editor, previous-era cart, or renderer dependency; reviewed the explicit Section B status/diff scope and prepared only validation/topology source, focused test/build hunks, report, and execution-state paths for the required feat commit.
Step 28 — created wall_mutation.zig with one public applyCommand transaction boundary, borrowed typed command payloads covering the planned wall operation families, mutable-source atomicity documentation, and private allocator-owned forward/inverse patch builders; no endpoint, split, opening, or patch helper is publicly exposed, and the reopened file is diff-clean.
Step 29 — wired wall_mutation as a single-owner focused test module and added the red draw-wall matrix for empty-source creation, exact coincident reuse, explicit magnet reuse, one-unit-nearby non-reuse, fractional DTO rejection, arbitrary lattice angles, reversed drag/side-A direction, and stale-revision atomicity; success cases require command-derived IDs and mutation receipts, rejection cases require typed codes and byte-shape preservation, and reopened test/build changes are diff-clean.
Step 30 — implemented atomic whole-u wall drawing with exact coordinate reuse, explicit same-floor magnet targets, no proximity guessing, bounded command-derived IDs, typed structural-number/stale/catalog rejections, deep-owned candidate cloning, revision advancement, canonical record snapshots, SHA-256 before/after hashes, dependency-safe forward/inverse patches, thickness-aware half-open floor bounds, and complete dirty targets; version 1 draws require explicit absolute support, reopened source/tests are diff-clean, and the pinned Zig 0.16 ReleaseFast allocator harness passes 37/37.
Step 31 — added black-box crossing traps for one lattice X, one endpoint-on-edge T, one stroke through three edges, reverse-stroke intersection identity/direction order, atomic off-lattice rational rejection, and atomic collinear-overlap rejection; the pinned Zig 0.16 red run compiles and isolates exactly these six failures while all 37 prior tests remain green, and reopened assertions avoid unsafe optional unwraps.
Step 32 — implemented exact ordered intersection planning with wall_topology predicates, typed off-lattice/collinear rejection, drag-direction cut ordering, stable-ID ordering for crossed source edges, deterministic endpoint/intersection/child IDs, atomic predecessor replacement, complete receipt snapshots and reversible patch operations, and conservative affected-bound union; X/T/multi/reversed children preserve each predecessor’s directed side-A/B finishes, reopened source/build/tests are format/diff clean, and the pinned Zig 0.16 ReleaseFast allocator harness passes 43/43.
Step 33 — added allocator-owned split-child fixtures proving one measured opening fully before column 32 remains on child zero, one fully after remaps to child one with adjusted column, an occupied cell on the junction rejects atomically, and stable wall anchors on both ranges remap edge/column plus receipt rows; the pinned Zig 0.16 red run passes 44/46 with only the two blanket-rejection success cases failing, and reopened fixtures are format/diff clean.
Step 34 — implemented exact child-edge lineage with complete-distance column starts, catalog-owned occupied/clearance range validation, stable opening partition and local-column adjustment, stable anchor edge/column adjustment, canonical opening/anchor record deltas, and anchor replacements in both patches; allocation occurs only after rejection validation, reopened source/tests/build wiring are format/diff clean, and the pinned Zig 0.16 ReleaseFast allocator harness passes 46/46 with zero leaks.
Step 35 — added an owned opening-slot query scaffold and 11 red tests covering insert, move, delete, measured-kit configure, facing/hinge configure, complete row/column slot ordering, identical interactive/procedural hashes, disjoint sparse masks, end bounds, occupied/clearance collisions, thickness/profile incompatibility, and measured height versus half walls; all fixtures derive dimensions from validated catalog entries, the pinned Zig 0.16 run preserves 46 prior passes and fails exactly the 11 new boundaries, and reopened sources are format/diff clean.
Step 36 — implemented one native measured-opening kernel for ordered slot enumeration and insert/move/delete/configure commands, with exact whole-u input, command-derived IDs, typed family/kind/profile/thickness/height/bounds rejections, sparse/full occupied masks, required-clear collisions, stable source identity, candidate validation, edge/opening deltas, reversible edge patches, target-specific dirtiness, and widened cell arithmetic; reopened source/tests are format/diff clean and the pinned Zig 0.16 ReleaseFast allocator harness passes 57/57 with zero leaks.
Step 37 — added seven red edge-authority tests for exact integer absolute base/height/thickness, full-to-half-to-full profile mutation, typed catalog style replacement, independent directed side-A/B finishes, single-edge orphan cleanup, final-edge/final-vertex cleanup, and explicit vertex deletion cascading all incident edges and newly orphaned vertices; the pinned Zig 0.16 run preserves 57 prior passes and fails exactly these seven operation tags, and reopened tests are format/diff clean.
Step 38 — implemented atomic edge dimension/profile/style/side-finish transactions with stable IDs, exact whole-u validation, typed catalog authority, existing-opening revalidation, reversible edge deltas, targeted dirty outputs, and conservative before/after bounds; edge and vertex deletion now remove incident openings/anchors plus only newly orphaned endpoint vertices in dependency-safe patches. Reopened source is format/diff clean and the pinned Zig 0.16 ReleaseFast allocator harness passes 64/64 with zero leaks.
Step 39 — added seven independent receipt round-trip traps covering empty-source draw, predecessor splitting, measured opening insertion, edge finish edit, dependency-ordered deletion, anchor attach, and anchor detach; each applies a receipt’s forward then inverse patch to a separately constructed identical source and requires restoration of its original revision and canonical source hash. After correcting two fixture inputs, the pinned Zig 0.16 red run preserves 64 prior passes and fails exactly the seven unimplemented patch/anchor boundaries.
Step 40 — made wall_types.zig the sole canonical format owner with stable-ID source serialization, stable opening order, checked enum/length decoding, trailing-byte rejection, and recursive record ownership; implemented atomic exact-before-byte patch application, dependency-safe owned record insertion/removal/replacement, canonical applied-patch receipts with automatic inverse construction, and native exact-u anchor attach/detach using deterministic `:a:0` IDs. Reopened source/contracts are format/diff clean and the pinned Zig 0.16 ReleaseFast allocator harness passes 71/71 with every forward/inverse round trip restoring revision and canonical hash.
Step 41 — replaced the facade’s placeholder command envelope with the native typed operation/command/result surface, added a recursive wall-source clone owner, and made facade application validate original structure/catalog/topology, mutate an owned candidate, validate its resulting DCEL, then swap only on success. A crafted duplicate-span patch proves post-mutation topology rejection leaves the caller’s revision and records unchanged; reopened facade/types/tests/build wiring are format/diff clean and the pinned ReleaseFast allocator harness passes 73/73.
Step 42 — audited the focused mutation suite’s allocator construction, reran the repository-pinned Zig 0.16 ReleaseFast architecture target, and recorded timestamp, command, exit, runtime, cache state, all 73 passing cases, and the zero-leak ownership result in S028-043-mutation.md; all mutation transactions use std.testing.allocator directly and the sole arena is the pre-mutation smoke test backed by that allocator.
Step 43 — reopened every receipt, rejection, patch operation, remap, face-lineage, canonical-record, candidate-source, and opening-slot owner/deinit path and recorded the recursive allocation map in S028-043-mutation.md; format/diff checks and the 73/73 allocator gate remain clean, and only explicit Section C paths are prepared for the required logical commit.

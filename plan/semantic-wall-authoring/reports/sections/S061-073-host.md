# Section E — Versioned host boundary

## JavaScript boundary proof

Command:

```text
tools/v8cli /tmp/editor-architecture-host.test.js
```

Result: exit 0; **8 passed, 0 failed**.

The cases cover the native empty-source golden bytes, measured catalog install and
structured query packets, all 19 stable rejection codes, stale revision evidence,
both directions of declared-length mismatch, result-kind mismatch, absent native
capability, and shared protocol constants.

## Native wire proof

Command:

```text
tools/zig/zig build test-building-architecture -Doptimize=ReleaseFast --summary all
```

Result: exit 0; **121/121 tests passed**.

The native proof exercises the bounded RJAW codec, all rejection tags, zero-copy
result ownership, measured catalog installation and query, source validation, and
an actual draw-wall mutation decoded back into revision 1 with the canonical edge
identifier `host-draw:e:0`. Partial catalog/source decode paths initialize every
owned field before filling it, and installed catalog ownership crosses the service
boundary exactly once even if result encoding subsequently fails.

## ReleaseFast editor build

Command:

```text
SHIP_RUN_PACKAGE=0 ./tools/rjit ship editor
```

Result: exit 0. The production ship bundled the editor, enabled the `game-build`
ingredient, compiled and linked the V8 host in `ReleaseFast`, skipped only the
self-extracting package as requested, and produced
`zig-out/bin/editor` (**407,912,424 bytes**).

## Shared field audit

Reopened together:

- Native: `framework/game/architecture_wire.zig`
- Editor: `cart/editor/world/architectureHost.ts`
- Governing contract: `plan/semantic-wall-authoring/contracts/wire.md`

Every shared envelope identity, size, alignment, and resource limit is an exact
three-way match:

| Field | Native Zig | Editor TypeScript |
|---|---:|---:|
| magic | `0x57414a52` | `0x57414a52` |
| packet version | `1` | `1` |
| header bytes | `40` | `40` |
| section-directory bytes | `24` | `24` |
| section alignment | `8` | `8` |
| maximum packet bytes | `67,108,864` | `67,108,864` |
| maximum sections | `64` | `64` |
| maximum string bytes | `4,194,304` | `4,194,304` |
| maximum string count | `262,144` | `262,144` |
| maximum catalog entries | `65,536` | `65,536` |
| maximum vertices | `1,048,576` | `1,048,576` |
| maximum edges | `1,048,576` | `1,048,576` |
| maximum openings | `1,048,576` | `1,048,576` |
| maximum anchors | `1,048,576` | `1,048,576` |
| maximum output rows | `8,388,608` | `8,388,608` |

Both implementations also use little-endian integers, zero reserved fields and
padding, exact declared lengths, sorted 24-byte directory rows, and eight-byte
aligned non-overlapping sections. The packet-kind range is 1–22, family range is
0–4, target range is 0–12, and all section and rejection numeric tags reopen as
equal on both sides. The JavaScript proof rejects absent capabilities and malformed
native results; the native proof rejects malformed requests before semantic work.

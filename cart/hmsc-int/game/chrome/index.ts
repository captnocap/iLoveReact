// game/chrome/ — GAME_CHROME: the lab chrome kit (Chip/Knob/MeterRow/panel) +
// the lab environment (skybox/lights/ground) every lab gets for free (V14).
// CAPTURE PENDING.
//
// Rewritten from the Chip/Knob/panel + environment re-rolls across the lab
// corpus (the extraction map's last row). Door only, nothing fake.

export const GAME_CHROME = Object.freeze({
  status: 'capture-pending' as const,
});

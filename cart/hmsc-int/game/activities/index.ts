// game/activities/ — GAME_ACTIVITIES: repeatable side loops — the non-mission
// gameplay verbs (V22: role, rob, chase, evade, race, jump, accumulate; game
// modes are distribution presets of the V21 machine). CAPTURE PENDING. Door
// only, nothing fake.

export const GAME_ACTIVITIES = Object.freeze({
  status: 'capture-pending' as const,
});

// game/kinds/ — GAME_KINDS: tile/prop/NPC/role/landform registries (V4).
// PLACEHOLDER DOOR ONLY — the kinds capture is OWNED BY ANOTHER LANE (WO-2);
// nothing in this directory is written from this lane. The door exists so the
// V17 standard import line already resolves.

export const GAME_KINDS = Object.freeze({
  status: 'capture-pending' as const,
});

# CENSUS — /log (e2e walked 2026-06-06, session CENSUS-SET-0606-log)
Status today: /log is the in-app churn diagnostics tail for hmsc-int paint/editor performance work. It reads the perfLog ring, shows newest lines first, and exposes only three controls: key-only filtering, logging pause/resume, and clear.
## Capabilities (e2e-walked, line-referenced)
| id | what it does (user-visible behavior) | how (control/hotkey/gesture) | code | persistence | workbench accounting |
| C1 | Opens as a full-shell overlay while the editor stays mounted underneath. | Chrome Activity nav, headless boot route `/log`; selfshot `/tmp/census-log-boot.png`. | cart/hmsc-int/index.tsx:803, cart/hmsc-int/index.tsx:843, cart/hmsc-int/index.tsx:915, cart/hmsc-int/shell/chrome.tsx:164 | router hot key `hmsc-int:route` | PENDING workbench step 9 settings domains + logs (cart/hmsc-int/editors/workbench/sources.ts:20) |
| C2 | Shows the churn log header, visible line counts, path hint, and newest-first rows. | View `CHURN LOG`; clean walk row 0. | cart/hmsc-int/LogView.tsx:75, cart/hmsc-int/LogView.tsx:81, cart/hmsc-int/LogView.tsx:82, cart/hmsc-int/LogView.tsx:92, cart/hmsc-int/LogView.tsx:101 | in-memory perfLog ring plus diagnostics file path from telemetry | PENDING step 9 logs source |
| C3 | Toggles between key-only signal lines and all log lines. | Click `key only`; clean walk row 1. The frame after click visibly changed the label to `all lines`. | cart/hmsc-int/LogView.tsx:37, cart/hmsc-int/LogView.tsx:68, cart/hmsc-int/LogView.tsx:72, cart/hmsc-int/LogView.tsx:85 | twig `/log.keyOnly`; live readback showed `false` after the walk | PENDING step 9 logs source |
| C4 | Pauses and resumes churn diagnostic logging. | Click `paused` then `logging`; clean walk rows 2 and 5, assertions rows 3 and 6. | cart/hmsc-int/LogView.tsx:69, cart/hmsc-int/LogView.tsx:86, cart/hmsc-int/perfLog.ts:55, cart/hmsc-int/perfLog.ts:57 | telemetry diagnostic channel state, not a route twig | PENDING step 9 logs action |
| C5 | Clears the in-memory and on-disk diagnostic log. | Click `clear`; clean walk row 4. | cart/hmsc-int/LogView.tsx:87, cart/hmsc-int/perfLog.ts:52, cart/hmsc-int/perfLog.ts:53 | clears perfLog ring and telemetry diagnostics file | PENDING step 9 logs action |
| C6 | Shows an empty-state message that reflects logging state. | Live selfshot showed `logging paused`; clean walk observed `logging`/`paused` state changes. | cart/hmsc-int/LogView.tsx:96, cart/hmsc-int/LogView.tsx:98 | none beyond logging channel state | PENDING step 9 logs lens |
| C7 | Subscribes to log flushes so the route tails live without manual refresh. | Source cross-check; no separate hotkey. | cart/hmsc-int/LogView.tsx:65, cart/hmsc-int/LogView.tsx:67, cart/hmsc-int/perfLog.ts:48, cart/hmsc-int/perfLog.ts:86 | subscription only; ring/file persistence comes from perfLog | PENDING step 9 logs source |
## Hotkey inventory
No route-local hotkeys found in `LogView.tsx`. Access is through the Chrome Activity icon.
## Persistence inventory (what survives reload, via what)
`keyOnly` survives route leave/return and hot reload through `useRouteTwigState('/log', 'keyOnly', true)` and `_route-twigs.json` (cart/hmsc-int/LogView.tsx:68, cart/hmsc-int/editors/twigs.ts:66). The log rows themselves are a bounded in-memory ring plus telemetry diagnostics file, not a route-authored stream (cart/hmsc-int/perfLog.ts:44, cart/hmsc-int/perfLog.ts:50, cart/hmsc-int/perfLog.ts:59). Clear wipes both ring and diagnostics; logging on/off uses `GAME_TELEMETRY.setDiagnosticChannel('churn', on)`.
## Oddities/bugs found while walking (report, do not fix)
The first log walk's query assertion for `all lines` failed even though the captured frame after clicking visibly showed `all lines`; the clean walk removed that brittle assertion and all scripted control steps passed. The headless host emits an existing watchdog/crash trailer after some autotest exits even when the manifest is complete.
## Unverified-live rows (code says it exists, could not trigger; why)
Live non-empty log row coloring and scroll were not triggered during this census because no paint/perf event stream was generated inside the route walk after clearing the log. Source confirms row color mapping and newest-first rendering at cart/hmsc-int/LogView.tsx:24, cart/hmsc-int/LogView.tsx:45, and cart/hmsc-int/LogView.tsx:101.

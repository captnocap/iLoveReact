# CENSUS — /settings (e2e walked 2026-06-06, session CENSUS-SET-0606-settings)
Status today: /settings is the grand settings overlay for the editor shell. It shows a read-only fold of route session activity on the left and the P2 tunables registry on the right; in today's live walk the settings route rendered, but its session bus store reported a corrupt sessions stream and therefore only showed the empty/all state.
## Capabilities (e2e-walked, line-referenced)
| id | what it does (user-visible behavior) | how (control/hotkey/gesture) | code | persistence | workbench accounting |
| C1 | Opens as a full-shell overlay while the base editor stays mounted underneath. | Chrome Settings nav, headless boot route `/settings`; selfshot `/tmp/census-settings-boot.png`. | cart/hmsc-int/index.tsx:803, cart/hmsc-int/index.tsx:844, cart/hmsc-int/index.tsx:945, cart/hmsc-int/shell/chrome.tsx:165 | router hot key `hmsc-int:route`; route-local state below uses twigs | PENDING workbench step 9 settings domains + logs (cart/hmsc-int/editors/workbench/sources.ts:20) |
| C2 | Exits back to the editor overlay stack. | Click `← editor`; headless walk manifest `tests/screenshots/census-settings-walk/manifest.txt` row 10. | cart/hmsc-int/editors/settings/SettingsRoute.tsx:141, cart/hmsc-int/editors/settings/SettingsRoute.tsx:145 | none; navigation only | PENDING step 9 |
| C3 | Displays the session event bus summary and rows, newest first, folded from the V20 sessions stream. | View `SESSION EVENT BUS`; live walk saw `0 channels · 0 commits` plus store-unavailable warning. | cart/hmsc-int/editors/settings/SettingsRoute.tsx:94, cart/hmsc-int/editors/settings/SettingsRoute.tsx:99, cart/hmsc-int/editors/settings/SettingsRoute.tsx:151, cart/hmsc-int/editors/settings/bus.ts:41 | sessions stream/snapshot via `editorSessions().state()`; no new persistence | PENDING step 9 settings domains + logs |
| C4 | Filters the bus by channel chip; `all` clears the filter. | Click `all · 0`; clean walk row 2. | cart/hmsc-int/editors/settings/SettingsRoute.tsx:101, cart/hmsc-int/editors/settings/SettingsRoute.tsx:155, cart/hmsc-int/editors/settings/SettingsRoute.tsx:156, cart/hmsc-int/editors/settings/bus.ts:81 | twig `/settings.channelFilter`; live readback in `cart/hmsc-int/sessions/_route-twigs.json` | PENDING step 9 |
| C5 | Polls session undo point and tunable revision so the bus/knob view updates without route remount. | Live route remained mounted while knob clicks changed visible values; no manual hotkey. | cart/hmsc-int/editors/settings/SettingsRoute.tsx:76, cart/hmsc-int/editors/settings/SettingsRoute.tsx:79, cart/hmsc-int/editors/settings/SettingsRoute.tsx:90 | none by itself; it observes sessions/tunables | PENDING step 9 |
| C6 | Lists registered tunable systems and numeric knobs from the P2 registry. | View `TUNABLES · 6 systems · 63 knobs`; selfshot and clean walk rows 9. | cart/hmsc-int/editors/settings/SettingsRoute.tsx:104, cart/hmsc-int/editors/settings/SettingsRoute.tsx:105, cart/hmsc-int/editors/settings/SettingsRoute.tsx:187, cart/hmsc-int/editors/tunables.ts:69 | registry is live in memory; persisted overrides ride the tuning stream | ACCOUNTED workbench roster/panel via `tunablesSource` today; full settings still PENDING step 9 (cart/hmsc-int/editors/workbench/tunablesSource.ts:21) |
| C7 | Edits tunables in place through knob decrement/increment controls. | Click visible knob buttons at `@581,136`, `@675,136`, `@581,167`, `@675,167`; clean walk rows 3, 5, 7, 8. | cart/hmsc-int/editors/settings/SettingsRoute.tsx:120, cart/hmsc-int/editors/settings/SettingsRoute.tsx:121, cart/hmsc-int/editors/settings/SettingsRoute.tsx:203, cart/hmsc-int/editors/tunables.ts:124 | tuning stream `set` events and `tuning.snapshot.json`; representative prior rows visible in `cart/hmsc-int/data/streams/tuning.jsonl` | ACCOUNTED workbench panel write-through via `tunablesSource.set`; route commit history PENDING step 9 |
| C8 | Resets a non-default tunable back to its registered default. | Click reset area after making a value non-default; clean walk row 4. | cart/hmsc-int/editors/settings/SettingsRoute.tsx:128, cart/hmsc-int/editors/settings/SettingsRoute.tsx:207, cart/hmsc-int/editors/tunables.ts:180, cart/hmsc-int/editors/tunables.ts:216 | tuning stream `reset` removes override | PENDING step 9 for reset affordance in workbench |
| C9 | Registers its own settings-view tunables for bus poll interval, row cap, and column width. | Visible in source and snapshot text; offscreen/scrollable in live run. | cart/hmsc-int/editors/settings/SettingsRoute.tsx:36, cart/hmsc-int/editors/settings/SettingsRoute.tsx:43, cart/hmsc-int/editors/settings/SettingsRoute.tsx:46 | tuning stream if edited | ACCOUNTED workbench tunables roster/panel; PENDING settings route retirement step 9 |
## Hotkey inventory
No route-local hotkeys found in `SettingsRoute.tsx`. The only live route access observed is the Chrome Settings icon and the `← editor` chip.
## Persistence inventory (what survives reload, via what)
`channelFilter` survives route leave/return and hot reload through `useRouteTwigState('/settings', 'channelFilter', null)` and `_route-twigs.json` (cart/hmsc-int/editors/settings/SettingsRoute.tsx:101, cart/hmsc-int/editors/twigs.ts:66). Tunable edits persist through `tuningStream` set/reset events and snapshots (cart/hmsc-int/editors/tunables.ts:201, cart/hmsc-int/editors/tunables.ts:207). Session bus rows persist through the global `sessions` stream, but today's route walk hit a corrupt sessions record, so non-empty bus persistence was source-confirmed rather than cleanly live-confirmed.
## Oddities/bugs found while walking (report, do not fix)
The live `/settings` selfshot showed `store unavailable: data store: corrupt record at cart/hmsc-int/data/streams/sessions.jsonl:884`, leaving the bus empty even though the page rendered. The generated witness snapshot can see and click many underlying editor controls after route navigation, so proof manifests must be scoped carefully; the clean walk only used assigned route controls.
## Unverified-live rows (code says it exists, could not trigger; why)
Non-empty per-channel chips from `channels.map(...)` were not verified live because the route's session store failed open with the corrupt sessions record (cart/hmsc-int/editors/settings/SettingsRoute.tsx:157). All offscreen repeated tunable rows were not individually clicked; the live walk exercised the common knob/reset code path and scroll behavior, while the source establishes the same renderer for every registry entry.

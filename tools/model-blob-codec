#!/usr/bin/env bash
# Build-and-run boundary for the canonical native RJMD reader/writer.
set -euo pipefail

ROOT="$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd -P)"
cd -- "$ROOT"
exec "$ROOT/tools/zig/zig" build model-blob-codec -Doptimize=ReleaseFast -- "$@"

//! Engine-side tests for editor-live cooked-door identity/state reconciliation.
//! Run: zig build test-world-live-mesh-doors

const live_mesh_doors = @import("world_live_mesh_doors");

test "live mesh door module tests" {
    _ = live_mesh_doors;
}
